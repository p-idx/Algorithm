#include <iostream>
#include <string>
#include <vector>

int KMPfind(const std::string& s, const std::string& pat, int index = 0)
{
	// for failure func
	static std::vector<int> pi(pat.size());
	for (auto &e : pi) e = -1;


}


int main()
{
	return 0;
}